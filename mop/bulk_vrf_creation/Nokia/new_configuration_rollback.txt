/configure router policy-options begin
/configure router policy-options
            ~no community "target:{{import_rt}}" members "target:{{import_rt}}"
            ~no community "target:{{export_rt}}" members "target:{{export_rt}}"
            no policy-statement "{{vrf_name}}_Import"
            no policy-statement "{{vrf_name}}_Export"
/configure router policy-options commit

/configure service vprn {{vprn_number}} shutdown
/configure service no vprn {{vprn_number}}
/configure service no customer {{vprn_number}}