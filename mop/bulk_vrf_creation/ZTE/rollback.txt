config t
ip vrf {{vrf_name}}
  mpls label mode per-vrf
  address-family ipv4
    ~no route-target import {{import_rt}}
    ~no route-target export {{export_rt}}
  $
  address-family ipv6
    ~no route-target import {{import_rt}}
    ~no route-target export {{export_rt}}
  $
$