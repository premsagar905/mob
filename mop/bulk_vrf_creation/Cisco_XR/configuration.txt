vrf {{vrf_name}}
 address-family ipv4 unicast
  import route-target
   ~{{import_rt}} 
  export route-target
   ~{{export_rt}}
 !
 address-family ipv6 unicast
  import route-target
   ~{{import_rt}}
  export route-target
   ~{{export_rt}}
 !
!
router bgp 55410
 !
 vrf {{vrf_name}}
  rd {{rd_value}}:{{export_number}}
  address-family ipv4 unicast
   label mode per-vrf
   redistribute connected
   redistribute static
!
  address-family ipv6 unicast
   label mode per-vrf
   redistribute connected
   redistribute static
  !
 !
root